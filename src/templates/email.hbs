<!DOCTYPE html>
<html>
<head>
  <style>
    body {
      background-color: #F0F0F0;
      font-family: Arial, sans-serif;
    }
    .email-container {
      max-width: 600px;
      margin: 0 auto;
      background-color: #fff;
      border: 1px solid #ddd;
      padding: 20px;
      border-radius: 4px;
    }
    .header {
      background-color: #004d40;
      padding: 20px;
      color: white;
      text-align: center;
    }
    .content {
      margin: 20px 0;
      color: #333;
    }
    .footer {
      margin-top: 20px;
      font-size: 12px;
      color: #999;
    }
    .logo {
      max-width: 150px;
      height: auto;
    }
  </style>
</head>
<body>
  <div class="email-container">
    <div class="header">
      <img src="{{logo}}" alt="Logo de GreenItForward" class="logo"/>
      <h1>{{name}},</h1>
    </div>
    <div class="content">
      {{#if message}}
        <p>{{message}}</p>
      {{else}}
        <p>
            Nous vous envoyons ce mail pour vous féliciter de participer a l'application GreenItForward
        <p>
            Toute l'équipe vous remercie pour votre assiduité et vous souhaite une bonne journée
        </p>
      {{/if}}
    </div>
    <div class="footer">
      <p>Meilleures salutations,</p>
      <p>Équipe GreenItForward</p>
      <p>{{ourMailAdress}}</p>
    </div>
  </div>
</body>
</html>
